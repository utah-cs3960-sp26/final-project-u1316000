
- fix asset file structure

- need to start closing branches to avoid exploding growth
    - death?
    - story end?
    - merges?

- graph view
    -I am interested in creating a story graph view on the dev console on localhost:8001/graph or something. I want to see a dynamic branching tree of nodes. Each node should be a circle with its background being that node's background in the story. It should show the node title and the id, and the edges should show a truncated text of the choice option. clicking on a node should open a menu that allows me to open the actual scene in another tab or view more detailed info about the node.